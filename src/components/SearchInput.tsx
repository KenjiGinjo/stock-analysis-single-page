"use client";

import React from "react";
import router from "@/router";
export function SearchInput() {
  console.log(router.name);
  return (
    <div>
      <input type="text" />
    </div>
  );
}
